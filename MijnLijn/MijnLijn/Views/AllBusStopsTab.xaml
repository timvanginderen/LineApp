<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MijnLijn.Widgets;assembly=MijnLijn"
             x:Class="MijnLijn.AllBusStopsTab"
             Title="All stops">

    <StackLayout>

        <SearchBar x:Name="AllStopsSearchBar" TextChanged="AllStopsSearchBar_TextChanged"/>

        <ListView x:Name="AllStopsListView" 
			      ItemTapped="OnItemTapped"
			      ItemsSource="{Binding .}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <StackLayout Orientation="Vertical">
                                <Label Text="{Binding Name}" Font="15,Bold" />
                                <Label Text="{Binding NumbersDisplay}"/>
                            </StackLayout>
                            <local:FavoriteSwitch Toggled="Switch_Toggled" IsToggled="{Binding Favorited}" HorizontalOptions="EndAndExpand" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    </StackLayout>
</ContentPage>